<form method="post" action="{{ path('app_admin_carrier_delete', {'id': carrier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carrier.id) }}">
    <button class="btn">Delete</button>
</form>
